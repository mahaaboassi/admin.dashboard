/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "x535bynjt9dmmdla.public.blob.vercel-storage.com",
      // Add other domains here if needed
    ],
  },
};

export default nextConfig;
